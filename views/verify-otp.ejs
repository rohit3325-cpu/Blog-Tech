<!DOCTYPE html>
<html lang="en">
<head>
	<%- include('./partials/head') %>
	<title>Verify OTP</title>
</head>
<body>
	<%- include('./partials/navbar') %>
	<div class="container" style="max-width: 480px; padding: 60px 15px;">
		<div class="card" style="border-radius: 16px; box-shadow: 0 8px 25px rgba(0,0,0,0.1);">
			<div class="card-body">
				<h3 class="card-title mb-3 text-center">Email Verification</h3>
				<p class="text-muted text-center">We sent a 6-digit code to <strong><%= email %></strong></p>
				<% if (typeof error !== 'undefined') { %>
					<div class="alert alert-danger"><%= error %></div>
				<% } %>
				<form action="/user/verify-otp" method="post">
					<input type="hidden" name="userId" value="<%= userId %>">
					<div class="mb-3">
						<label class="form-label">Enter OTP</label>
						<input type="text" class="form-control" name="otp" maxlength="6" pattern="\\d{6}" required>
					</div>
					<button type="submit" class="btn btn-success w-100">Verify</button>
				</form>
			</div>
		</div>
	</div>
	<%- include('./partials/script') %>
</body>
</html>
